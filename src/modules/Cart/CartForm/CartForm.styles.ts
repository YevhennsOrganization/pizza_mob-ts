import { StyleSheet } from "react-native";

export const cartFormCSS = StyleSheet.create({
  form: {
    display: "flex",
    flexDirection: "column",
    gap: 5,
    width: "100%",
  },
  buttonText: {
    fontSize: 16,
    color: "white",
  },
});
