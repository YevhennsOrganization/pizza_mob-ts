import { StyleSheet } from "react-native";

export const CartListCSS = StyleSheet.create({
  cartList: {
    marginBottom: 20,
  },
  totalPayment: {
    // font-family: var(--secondary-font);
    marginBottom: 20,
    textAlign: "center",
  },
});
