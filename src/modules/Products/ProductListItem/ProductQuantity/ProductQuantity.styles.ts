import { StyleSheet } from "react-native";

export const productQuantityCSS = StyleSheet.create({
  wrapper: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: 5,
  },
  quantityText: {
    fontSize: 16,
  },
});
