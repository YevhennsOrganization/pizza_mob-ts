import { StyleSheet } from "react-native";

export const roundButtonCSS = StyleSheet.create({
  button: {
    backgroundColor: "transparent",
    borderRadius: 16,
    border: "none",
    // color: var(--accent-color);
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
});
